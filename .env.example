PORT=3001
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=admin
DB_PASSWORD=changeme
DB_DATABASE_NAME=snowmandb
JWT_SECRET=changeme
JWT_EXPIRE=216000
# true = allow any origin (current default). For production, set to the
# real frontend origin(s), comma-separated, e.g. https://dlh.example.com
CORS_ORIGIN=true
